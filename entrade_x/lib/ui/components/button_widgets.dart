import 'package:flutter/material.dart';

class ButtonWidget extends StatelessWidget {
  final String? name;
  final Widget? child;
  const ButtonWidget({super.key, this.name, this.child});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
